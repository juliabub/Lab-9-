#The authors' names are: Julia, Julia, and Christina

#Modifying Strings
string = "Hello, world!"
output = []
index = 0
while index < len(string):
    output.append(string[index])
    index += 1

print(output)
#This while loop adds the individual letters from string to the empty list output
